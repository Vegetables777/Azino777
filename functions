"""отсюда все файлы импортирутся в main
    , чтобы импортировать что-то в другой файл, нужно в начале main'а прописать
     import functions as fc.
     чтобы вызвать функцию draw из этого
     файла в main'е нужно прописать:
     fc.draw(args)"""
